require("dotenv").config({ path: "./.env" }); // Use your custom filename

const accessKey = process.env.ACCESS_KEY;
const secretKey = process.env.SECRET_KEY;

// console.log(`Access Key: ${accessKey}`);
// console.log(`Secret Key: ${secretKey}`);
